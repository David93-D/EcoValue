import { CriptoFilterPipe } from './cripto-filter.pipe';

describe('CriptoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CriptoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
