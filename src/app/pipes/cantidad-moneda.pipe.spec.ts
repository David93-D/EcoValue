import { CantidadMonedaPipe } from './cantidad-moneda.pipe';

describe('CantidadMonedaPipe', () => {
  it('create an instance', () => {
    const pipe = new CantidadMonedaPipe();
    expect(pipe).toBeTruthy();
  });
});
